# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# IMPORTANT: Use a strong password and never commit this file to git!
MONGO_URI=your_mongodb_connection_string_here
DATABASE=your_database_name_here

# JWT Configuration
# IMPORTANT: Generate a secure random string (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_here
JWT_EXPIRE=1d

# CORS Configuration (Production)
# Comma-separated list of allowed origins (no spaces!)
# Example: https://app.example.com,https://admin.example.com
# Leave empty in development to allow all origins
# IMPORTANT: Set this in production to prevent unauthorized access
ALLOWED_ORIGINS=

# SMS Service Configuration (Twilio)
# Sign up at: https://www.twilio.com/try-twilio
# Find credentials at: https://console.twilio.com/
# IMPORTANT: Required for OTP and PIN delivery
TWILIO_ACCOUNT_SID=your_twilio_account_sid_starts_with_AC
TWILIO_AUTH_TOKEN=your_twilio_auth_token_32_characters
TWILIO_PHONE_NUMBER=your_twilio_phone_number_with_plus_sign

# Alternative: AWS SNS (uncomment if using instead of Twilio)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Test Database (for integration tests)
# This should be a separate database from production
TEST_MONGO_URI=mongodb://localhost:27017/laundro_test

# Optional: Username for admin operations
USERNAME=admin

# SECURITY REMINDERS:
# 1. Never commit config.env to version control (it's in .gitignore)
# 2. Use environment variables or secrets management in production
# 3. Rotate credentials regularly (JWT: 6mo, DB: 3mo, API keys: 12mo)
# 4. Generate strong passwords: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
